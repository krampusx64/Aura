services:
  aurago:
    build:
      context: .
      dockerfile: Dockerfile
    image: aurago:latest
    container_name: aurago

    # --- Port mapping ---
    # Change the host-side port (left) if 8088 is already in use on your machine.
    ports:
      - "8088:8088"

    # --- Volumes ---
    volumes:
      # The config.yaml file is auto-generated by the container if it doesn't exist.
      # You can edit it directly here or via the Web UI later.
      - ./config.yaml:/app/config.yaml

      # Persistent runtime data: memory, chat history, state, master key, vector DB.
      - aurago_data:/app/data

      # Python venv + generated tools.
      # Mount this as a named volume so installed pip packages survive restarts.
      - aurago_workdir:/app/agent_workspace/workdir

      # Optional: expose the agent's tool scripts on the host for inspection.
      # - ./agent_workspace/tools:/app/agent_workspace/tools

    # Note: AURAGO_MASTER_KEY is generated automatically on first run
    # and saved securely in your aurago_data volume at /app/data/.env

    # --- Restart policy ---
    restart: unless-stopped

    # --- Logging ---
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  aurago_data:
  aurago_workdir:
